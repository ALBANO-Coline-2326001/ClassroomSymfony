{% extends 'base.html.twig' %}

{% block title %}Mes Cours{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>📚 Mes Cours</h1>
            <div>
                <a href="{{ path('teacher_dashboard') }}" class="btn btn-outline-secondary me-2">
                    <i class="fas fa-arrow-left"></i> Tableau de bord
                </a>
                <a href="{{ path('app_cours_new') }}" class="btn btn-success">
                    <i class="fas fa-plus"></i> Créer un cours
                </a>
            </div>
        </div>

        <div class="row">
            {% for c in cours %}
                <div class="col-md-4 mb-4">
                    <div class="card h-100 shadow-sm border-0">
                        <div class="card-header bg-white border-bottom-0 pt-4 pb-0">
                            <div class="badge bg-primary float-end">Prof. {{ c.teacher.lastName }}</div>
                        </div>
                        <div class="card-body">
                            <h5 class="card-title fw-bold text-primary">{{ c.title }}</h5>
                            <p class="card-text text-muted small">
                                {{ c.contenu|length > 100 ? c.contenu|slice(0, 100) ~ '...' : c.contenu }}
                            </p>

                            <hr>

                            <div class="d-grid gap-2">
                                <a href="{{ path('app_cours_show', {'id': c.id}) }}" class="btn btn-outline-primary fw-bold">
                                    <i class="fas fa-eye me-1"></i> Voir & Gérer Contenu
                                </a>

                                <a href="#" class="btn btn-warning text-dark fw-bold btn-sm">
                                    <i class="fas fa-edit me-1"></i> Modifier infos
                                </a>
                            </div>
                        </div>
                        <div class="card-footer bg-light text-muted small text-center">
                            {{ c.documents|length }} Documents • {{ c.videos|length }} Vidéos
                        </div>
                    </div>
                </div>
            {% else %}
                <div class="col-12 text-center py-5">
                    <div class="text-muted mb-3"><i class="fas fa-folder-open fa-3x"></i></div>
                    <h3>Vous n'avez pas encore créé de cours.</h3>
                    <a href="{{ path('app_cours_new') }}" class="btn btn-primary mt-3">Commencer maintenant</a>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}